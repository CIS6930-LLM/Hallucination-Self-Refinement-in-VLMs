# Tests

Place unit and integration tests here. Follow the directory structure of `src/` and prefer `pytest` for Python components. Include lightweight fixtures for synthetic samples to validate self-refinement flow.
